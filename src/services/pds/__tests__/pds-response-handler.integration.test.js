//import { parsePdsResponse } from '../pds-response-handler';

describe('pds-response-handler', () => {
  describe('parsePdsResponse', () => {
    it('should return an object', () => {
      //expect(parsePdsResponse()).toEqual({});
    });
  });
});
